# Backend environment example for voice-tts-kr
#
# Copy this file to `.env` in the `backend/` directory and adjust values as needed.

# --- Provider feature flags ---

# Enable the mock tone provider (1 = enabled, 0 = disabled).
MOCK_TONE_ENABLED=1

# Enable the Coqui TTS provider (1 = enabled, 0 = disabled).
COQUI_ENABLED=0

# --- Coqui TTS configuration ---

# Model name to load for Coqui TTS.
COQUI_MODEL_NAME=tts_models/en/ljspeech/tacotron2-DDC

# Language tag used for Coqui where applicable.
COQUI_LANGUAGE=en-US

# --- Server configuration (optional) ---

# Port for the FastAPI app (used by uvicorn).
PORT=8080

